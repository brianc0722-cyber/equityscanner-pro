# EquityScanner Pro - Render Deployment (Lightweight for free tier)
# Heavy ML (torch + transformers) removed — causes builds to fail or timeout
# Pre-market engine runs in statistical/demo mode using scikit-learn only

fastapi>=0.115
uvicorn[standard]>=0.30
streamlit>=1.38

typer[all]>=0.12
rich>=13.0

aiohttp>=3.10
websockets>=12.0
redis>=5.0

pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
matplotlib>=3.9

prometheus-client>=0.20

python-dotenv>=1.0
pydantic>=2.8
pydantic-settings>=2.4
